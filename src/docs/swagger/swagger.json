{
  "openapi": "3.0.0",
  "info": {
    "title": "Phoenix Payment Service API",
    "version": "1.0.0",
    "description": "OpenAPI definition for the Phoenix Payment microservice. Expand `paths` with full schemas and operations as the API evolves."
  },
  "servers": [
    {
      "url": "/",
      "description": "Current server (same origin as this UI)"
    }
  ],
  "paths": {},
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ]
}

